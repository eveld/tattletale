{
  "name": "tattletale",
  "version": "1.0.0",
  "description": "Twitch chat collector",
  "author": "techn9cs",
  "main": "tattletale.js",
  "scripts": {
    "test": "istanbul test _mocha --require test/helpers/* --report lcovonly -- -R spec test && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:technics/tattletale.git"
  },
  "dependencies": {
    "node-etcd": "^3.0.2",
    "bunyan": "^1.3.4"
  },
  "devDependencies": {
    "mocha": "^2.1.0",
    "chai": "^2.1.0",
    "sinon": "^1.12.2",
    "coveralls": "^2.11.2",
    "mocha-lcov-reporter": "^0.0.2",
    "istanbul": "^0.3.6"
  }
}